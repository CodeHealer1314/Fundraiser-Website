spring.application.name=backend

# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/fundraising_db
spring.datasource.username=root
spring.datasource.password=your_password_here
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate - auto creates/updates tables based on your entity classes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server
server.port=8080

# JWT
app.jwt.secret=fundraising_super_secret_key_make_this_long_and_random
app.jwt.expiration=86400000
